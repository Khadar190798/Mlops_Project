from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The project trains a simple machine-learning model (e.g., Linear Regression, RandomForest, or any classifier) and uses MLOps tools to manage:Data versioningPipeline automationExperiment trackingModel packaging & storageThis makes the workflow production-ready and scalable.',
    author='Shaik Khadar Vali',
    license='MIT',
)
